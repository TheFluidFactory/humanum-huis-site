/*
 * Humanum Huis Mirror capture endpoint.
 * After deploying the bound Google Apps Script web app, replace the empty string
 * with its /exec URL. Keep this file public: it contains no credentials.
 */
window.HRM_SUBMISSION_ENDPOINT = "";
